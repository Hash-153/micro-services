apiVersion: apps/v1
kind: Deployment
metadata:
  name: fulfillment-service
  namespace: novacommerce
  labels:
    app: fulfillment-service
    tier: microservice
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fulfillment-service
  template:
    metadata:
      labels:
        app: fulfillment-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8006"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: novacommerce-workload-sa
      containers:
        - name: fulfillment-service
          image: novacommerce/fulfillment-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8006
              name: http
            - containerPort: 50056
              name: grpc
          envFrom:
            - configMapRef:
                name: novacommerce-global-config
            - secretRef:
                name: novacommerce-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: fulfillment-service-svc
  namespace: novacommerce
  labels:
    app: fulfillment-service
spec:
  type: ClusterIP
  selector:
    app: fulfillment-service
  ports:
    - name: http
      port: 8006
      targetPort: 8006
    - name: grpc
      port: 50056
      targetPort: 50056
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: fulfillment-service-hpa
  namespace: novacommerce
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fulfillment-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
